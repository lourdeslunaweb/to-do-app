export type Task = {
    id: string,
    description: string,
    title: string,
    status: string
}

export type User = {
    id: string;
    email: string;
    name: string;
    password: string;
};